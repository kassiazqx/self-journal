# 🚀 部署指南 · 自我觉察日记 App

## 第一步：注册 Supabase（免费，约 5 分钟）

1. 打开 [https://supabase.com](https://supabase.com)，点击右上角 **Start your project** 注册账号

2. 创建新项目：
   - Organization：默认
   - Project name：`self-journal`（随意）
   - Database Password：设一个强密码，**记住它**
   - Region：选 **Southeast Asia (Singapore)**（离中国最近）
   - 点击 **Create new project**，等待约 2 分钟

3. 创建数据库表：
   - 进入项目后，左侧菜单点击 **SQL Editor**
   - 点击 **New query**
   - 打开本地的 `supabase-schema.sql` 文件，复制全部内容粘贴进去
   - 点击右上角 **Run** 按钮执行

4. 获取 API 密钥：
   - 左侧菜单点击 **Project Settings** → **API**
   - 复制 **Project URL**（形如 `https://xxxxx.supabase.co`）
   - 复制 **Project API keys** 下面的 **anon public** 密钥

---

## 第二步：配置本地环境变量

在 `self-journal` 文件夹下，**复制** `.env.example` 文件，重命名为 `.env`：

```
cp .env.example .env
```

用文本编辑器打开 `.env`，填入 Supabase 密钥：

```
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的_anon_public_密钥
VITE_APP_URL=https://你的线上域名.vercel.app
```

`VITE_APP_URL` 用于注册验证邮件、重置密码邮件等认证回跳地址。线上部署时这里必须填真实域名，不能留 `localhost`。

---

## 第三步：本地测试（可选，验证功能）

在终端进入项目文件夹：

```bash
cd /Users/kassia1/Desktop/个人/noteapp/self-journal
npm run dev
```

打开浏览器访问 `http://localhost:5173`，测试注册/登录/记录功能。

---

## 第四步：部署到 Vercel（免费，永久在线）

### 方法一：通过 GitHub（推荐）

1. 注册 [GitHub](https://github.com) 账号

2. 在终端执行：
   ```bash
   cd /Users/kassia1/Desktop/个人/noteapp/self-journal
   git init
   git add .
   git commit -m "初始版本"
   ```

3. 在 GitHub 创建新仓库（点 + → New repository），名字随意，**不要**勾选 Initialize

4. 按 GitHub 提示推送代码：
   ```bash
   git remote add origin https://github.com/你的用户名/仓库名.git
   git push -u origin main
   ```

5. 打开 [https://vercel.com](https://vercel.com)，用 GitHub 账号登录

6. 点击 **Add New Project** → 选择你的仓库 → **Import**

7. **重要：配置环境变量**
   - 在 Vercel 部署页面，展开 **Environment Variables**
   - 添加两个变量（和 `.env` 文件内容一样）：
     - `VITE_SUPABASE_URL` = 你的 Supabase URL
     - `VITE_SUPABASE_ANON_KEY` = 你的 anon key
     - `VITE_APP_URL` = 你的 Vercel 线上地址（例如 `https://self-journal-kassia.vercel.app`）
   - 点击 **Deploy**

8. 部署完成后，Vercel 会给你一个 `https://xxx.vercel.app` 网址，这就是你的 App 地址！

### 方法二：Vercel CLI（稍简单）

```bash
npm install -g vercel
vercel login
vercel --prod
```
按提示操作，记得在 Vercel 控制台配置环境变量。

---

## 第五步：手机添加到主屏幕（像 App 一样使用）

### iPhone / Safari：
1. 用 Safari 打开你的 Vercel 网址
2. 点击底部分享按钮（□↑）
3. 选择 **添加到主屏幕**
4. 点击 **添加**

### Android / Chrome：
1. 用 Chrome 打开网址
2. 点击右上角三个点菜单
3. 选择 **添加到主屏幕**（或"安装应用"）

**完成！** 桌面上会出现「觉察日记」图标，点击直接全屏打开，和原生 App 体验一样。

---

## 常见问题

**Q：保存失败怎么办？**
检查 `.env` 文件里的 Supabase URL 和 Key 是否正确，且 Supabase 中已运行 SQL 建表。

**Q：语音输入没反应？**
语音识别需要 HTTPS 环境（Vercel 已自动开启）。本地开发用 `localhost` 也支持。部分安卓机需要用 Chrome 浏览器。

**Q：数据安全吗？**
Supabase 启用了 Row Level Security，每个用户只能看到自己的数据，不会互相泄露。

**Q：以后要加新功能怎么办？**
修改代码后，推送到 GitHub，Vercel 会自动重新部署，全程无需手动操作。
